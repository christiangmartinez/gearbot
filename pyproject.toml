[project]
name = "gearbotCLI"
version = "0.0"
dependencies = [
    "playwright",
    "typer",
]
requires-python = ">=3.13"
authors = [{name = "Christian Martinez"}]
description = "Automated search for music gear"
readme = "README.md"

[project.scripts]
gearbot = "app.cli:app"
